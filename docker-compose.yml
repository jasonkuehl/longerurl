services:
  makealongerlink:
    build: .
    container_name: makealongerlink
    restart: unless-stopped
    ports:
      - "60320:60320"
    environment:
      - PORT=60320
    # Optional: Add to your existing network
    # networks:
    #   - your_network
